odl-cluster{
  akka {
    loglevel = "DEBUG"
    #log-config-on-start = on

    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
      debug{
        #autoreceive = on
        #lifecycle = on

      }
    }
    remote {
      log-received-messages = on
      log-sent-messages = on

      log-remote-lifecycle-events = off
      netty.tcp {
        hostname = "localhost"
        port = 2551
      }
    }

    cluster {
      seed-nodes = ["akka.tcp://opendaylight-rpc@localhost:2551"]

      auto-down-unreachable-after = 10s
    }
  }
}
unit-test{
  akka {
    loglevel = "DEBUG"
    loggers = ["akka.event.slf4j.Slf4jLogger"]
    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
    }
  }
}

memberA{
  akka {
    loglevel = "INFO"
    loggers = ["akka.event.slf4j.Slf4jLogger"]
    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
    }
    remote {
      log-received-messages = off
      log-sent-messages = off

      log-remote-lifecycle-events = off
      netty.tcp {
        hostname = "localhost"
        port = 2551
      }
    }

    cluster {
      seed-nodes = ["akka.tcp://opendaylight-rpc@localhost:2552"]

      auto-down-unreachable-after = 10s
    }
  }
}
memberB{
  akka {
    loglevel = "INFO"
    loggers = ["akka.event.slf4j.Slf4jLogger"]
    actor {
      provider = "akka.cluster.ClusterActorRefProvider"
    }
    remote {
      log-received-messages = off
      log-sent-messages = off

      log-remote-lifecycle-events = off
      netty.tcp {
        hostname = "localhost"
        port = 2552
      }
    }

    cluster {
      seed-nodes = ["akka.tcp://opendaylight-rpc@localhost:2552"]

      auto-down-unreachable-after = 10s
    }
  }
}
